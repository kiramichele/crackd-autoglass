/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images: {
    // if you ever embed Facebook or Google photos by URL later
    remotePatterns: [],
  },
};

export default nextConfig;
